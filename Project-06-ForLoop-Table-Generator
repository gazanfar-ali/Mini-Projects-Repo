#Table using FOR Loop
n = int(input('Enter the number for table: '))
r = int(input('Enter the range for table: '))

# Use range() to iterate over numbers from 1 to r
for i in range(1, r + 1):
    print(n, '*', i, '=', n * i)
